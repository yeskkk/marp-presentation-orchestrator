{
  "name": "marp-presentation-orchestrator",
  "version": "0.7.2",
  "private": true,
  "description": "Pinned Marp CLI runtime for fixed-theme PDF and source-directory delivery.",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "4.5.0"
  },
  "scripts": {
    "marp": "marp",
    "marp:version": "marp --version"
  }
}
