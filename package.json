{
  "name": "marp-presentation-orchestrator",
  "version": "0.5.0",
  "private": true,
  "description": "Unpinned Marp CLI runtime for PDF-only presentation production.",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "latest"
  },
  "scripts": {
    "marp": "marp",
    "marp:version": "marp --version"
  }
}
