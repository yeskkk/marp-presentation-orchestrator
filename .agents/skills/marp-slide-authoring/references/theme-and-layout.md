# Theme and layout

The project theme descends from Gaia but uses a white academic background, Chinese-friendly sans-serif fonts, blue hierarchy headings, readable tables, two-column CSS, warning/success boxes, and formula cards. Do not introduce per-slide ad hoc font sizes. Split dense slides instead of shrinking type.
