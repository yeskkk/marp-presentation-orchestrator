# Planner checklist

- User was told: one mandatory five-channel full-deck review, every reviewer reads the entire frozen deck, no reviewer recheck, no resolution lifecycle, 2–3 MCQs per course unit, no screenshots/model vision, and text-only worker references.
- TASK states `course` or `report`, delivery mode, and the selected production profile.
- Only the main agent wrote or revised top-level `TASK.md`; any delegated planner work is explicitly attributable.
- Every presentation and content unit has stable IDs, `global_meeting_number` where applicable, and `deck_local_ordinal`.
- One planner-approved batch plan contains all lesson semantics before any unit assignment is expanded.
- Unit workspaces are absent until queued on the critical path.
- The selected profile and gates agree: greenfield full/compact, three-stage migration, or two-stage targeted revision.
- Review/revision/release workspaces do not exist before their just-in-time gates.
- `TOOLCHAIN-LOCK.yaml` matches the installed Marp version and the smoke test is current.
- Reference-access, review, engine-incident, and model policies agree with TASK.
- Policy audit passes; only TASK.md uses a confirmation digest.
