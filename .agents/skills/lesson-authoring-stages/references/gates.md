# Stage gates

A gate records: stage, assignment path, submitted artifact, worker self-check, coordinator decision, reason, UTC and any reopened downstream stages. The coordinator may accept/reopen but cannot rewrite the planner assignment.
