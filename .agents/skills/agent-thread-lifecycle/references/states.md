# Thread states

`active`, `idle_reusable`, `close_requested`, `closed`, `failed`, `unknown_runtime_state`.

A thread can be reused only when its independence tags and role capability match the new planner-written assignment.
