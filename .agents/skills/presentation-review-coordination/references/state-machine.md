# Required sequence

`authoring → initial review → initial changes → incremental review → incremental changes → final review → terminal revision → release closure → release approved → finalized`

If a round has zero findings, the next required round still occurs. Terminal closure may use an unchanged source but remains a separate release event.
