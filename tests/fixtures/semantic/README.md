# 语义协议维护素材（不用于运行任务）

这些输入、输出和校准案例用于维护项目源代码：验证 schema、页面引用、路由和判断方法的回归。
它们含故意错误或特殊情境，不是好课件模板，不是模型运行时的方法库。guide 编译器不读取本目录。
fixture 的 ID、反馈版本和结果只在测试中有效，不能复制到真实任务充当已读或已执行证据。
维护者修改通用原则时，应同时检查应报和不应误报的情形；测试格式通过不等于真实模型判断正确。
