# 指南维护入口

本页和所链接的材料只供项目源码维护使用，不是任务运行的必读输入。

完整维护方法见 [语义指导的源码维护](development/SEMANTIC-MAINTENANCE.md)：
把经验归纳为角色判断原则，把确定性约束交给代码，把事故原文和校准素材保留在测试中。
技能正文以目标、输入、工作顺序、证据和输出组织；不堆砌旧例，不要求模型读取维护样例。

当前有十个独立语义 skill，但仍保留原来的六份 agent 定义和五个 reviewer 线程通道。
发布验证关注实际路由、单会话 TASK 阅读、资源隔离、结果协议与状态兼容；
不做 skill lint，不用长度、章节计数或关键词命中代替方法质量审读。

整体流程见 [WORKFLOWS](WORKFLOWS.md)，运行输入与回执见 [HOST-PROTOCOL](HOST-PROTOCOL.md)。
README 维护整体结构与各步骤运作方式，历史更新不应与现行规则并列相互冲突。
