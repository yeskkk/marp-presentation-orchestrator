# 缺陷登记

## BUG-20260914-03：把教学生活算例当作新闻事实核验，造成无效写作、审核和阻塞

- 用户明确纠正（2026-09-14）：这是教学课件，不是新闻报道。生活算例不必完全真实，不要求写出处或检查出处；已有的此类出处应删除。author 判断情境符合近几年的生活实际、reviewer 没有提出具体反对即可，不应让双方继续计较事件和数据的真实性。这是项目级问题，不限于本次港铁算例。
- 实际证据：linear-algebra-v7 的 p02-01 整稿编辑已完成，却因港铁公告原始 PDF 未读到而在冻结前阻塞；作者对 recent-real-world-examples 和 v7-user-repair 留下来源核验 issue。main 随后下载原公告核对票价，同样延续了不必要的核验工作。任务辅助指导还要求“可核验生活或经济场景”“事件日期、直接来源”和“现实来源”，将教学合理性错误升级为报道级真实性要求。
- 正确要求：允许编拟或简化贴近近几年生活的情境和数据；作者判断合理，审核者仅在发现具体不合理之处、数学错误或影响理解的条件缺失时提出意见。不要求证明真实发生、匹配真实票价或统计数据、检索近期新闻、读取原始公告，也不要求来源清单、核验记录或额外真实性审核轮次。题目需要的假设用自然学生语言表达，不能为免责声明堆字。
- 数学边界：仍检查变量、单位、必要条件、模型关系、计算及结论；情境可编拟不等于数学可随意。不把虚构情境包装成已核实的新闻事实，优先写成题设。此纠正针对生活算例，不据此删除数学定义或其他真正承担教学作用的资料。
- 修复范围：统一项目作者/五通道审核指南、brief 与反馈契约及控制层阻塞规则；清除把“近期生活实际”解释成强制真实来源核验的旧要求。当前任务新稿中的生活算例出处也应在正式作者修订中删除；保留历史报告与回执，通过授权要求变更处理旧来源核验 issue，不能篡改作者结论或手工清零。已交付 p01 按用户此前要求保持原样。
- 验收：合理编拟且数学正确的生活算例，无来源、未联网核验也能正常进入审核和交付；不得单因来源未读到、缺出处或无法证实真实发生而阻塞；具体的数学或情境合理性问题仍可正常提出和修正。
- 状态：已登记用户明确的项目级要求；尚未完成指南、运行契约及当前稿件的同步修复。主代理不再继续本次票价来源核验；后续生产须先同步这项授权变更，不沿用冲突的旧要求。

## BUG-20260913-01：确认后缺少显式容量补设入口

- 发现于：`tasks/linear-algebra-v7`，2026-09-13。
- 复现：已确认任务的 `provider.handle_limit` 为 null；用户后来明确授权 16，`runner capacity` 仍报告 `User has not configured an allowed handle limit`，现有命令不能登记此授权。
- 影响：真实宿主可用也无法恢复返修；直接改 YAML 会破坏确认快照校验。
- 修复：新增 `runner set-capacity`，仅在暂停且无未对账执行/创建时写入 `capacity.authorized` 事件；实际准入取授权和宿主上限的较小值；不修改配置快照或模型。
- 验证：`test_explicit_capacity_at_pause_preserves_confirmed_snapshot`、`test_capacity_change_rejects_outstanding_creation` 通过；实际任务已记录用户容量 16，runner 报告 limit=16、required_peak_handles=13。
- 状态：本地已修复并验证，尚未提交版本。

## BUG-20260913-02：已授权 TASK 文字变更无法进入有效会话上下文

- 复现：用户修订学生受众表达和证明深度后，磁盘 TASK 若改变，`Service.confirmed()` 会拒绝；`Service.confirm()` 对已确认任务只返回 already_confirmed，无法登记文字修订。仅登记 feedback 又使 task_context 继续优先传入旧 TASK。
- 影响：新作业收到含“保留正确证明”等旧要求；用户要求与任务正文冲突。`apply_delta` 有实现但正常控制入口无法产生该类授权变更。
- 修复：新增 `task amend-text`，只允许暂停、无未对账作业且 settings/runtime 不变的文字修订，保存 `task.text_amended` 不可变历史事件；后续首读提供新全文，已读会话只获授权差异；旧请求与回执按原版本验证，不追溯伪造阅读。
- 验证：`test_authorized_text_amendment_delivers_delta_and_preserves_snapshot` 及 task_context 测试 16 项通过。实际诊断已成功结束后应用；task.text_amended 事件 543 已记录，新上下文预览为 apply_delta 且包含少证明要求。预览不是派发或阅读回执。
- 状态：本地修复、针对性测试和实际任务应用完成，完整控制层测试通过。

## BUG-20260913-03：旧桥接未接受响应阻挡已经完成任务的恢复

- 复现：桥接日志中 `audience:a-5faad27329df42ab82eed30bff3f2e3c:7` 仍 accepted=0；数据库相应 attempt 已 succeeded、步骤 completed 且另有真实回执。直接 replay 旧响应触发 `Audience evidence is outside the dispatched source or invented`。
- 影响：无实际未完成作业却无法驱动新诊断；重发会重复历史工作。
- 修复：仅对 succeeded attempt 中具有完成时间和回执的 completed audience step，将被替代旧响应保存为 `reconciled_history/superseded_historical_response`；不将其标记 accepted，不改语义数据库，不重发。
- 验证：`test_completed_historical_step_is_archived_without_acceptance` 通过；实际旧请求已归档，新诊断已启动。
- 状态：本地已修复并验证，尚未提交版本。

## INCIDENT-20260913-01：主代理漏同步 TASK.md

- 类型：执行失误，不以程序缺口代替责任。
- 事实：主代理已登记本次反馈、撰写返修范围，但未先更新 TASK.md，随后启动了只读诊断。用户指出后暂停后续派发；本轮尚未启动五通道审核或作者改稿。
- 纠正：更新任务正文，清除证明深度冲突及过时状态，登记授权文字修订，确保后续 worker 实际收到。已派发诊断不伪称读过新版本。

## BUG-20260913-04：发布稿旧主题未给表格提供边界

- 证据：p01 发布 artifact `r-e38ec354d4414288a7b69d7215dc30b8` 的旧 theme.css 没有 table/th/td 规则，原第 10 页 Markdown 为合法三列表格。
- 影响：PDF 单元格与公式列难区分，合法 Markdown 不能保证阅读语义。
- 现状：项目现行 `src/mpres/control/theme.css` 与 `themes/mathist-academic.css` 已含 2px 表格边框和 10px/16px 留白；旧交付尚未迁移。
- 处理：本轮作者副本采用当前统一主题，返修后运行原生数学与布局检查。
- 状态：项目主题已有规则，p01 修复及实际交付验证待完成。

## BUG-20260913-05：桥接逐段输出查询扫描全部历史日志

- 证据：实际 wire 表 112165 行；按 request_id/direction 读取当前执行消息的查询计划为 SCAN wire，单次约 0.239 秒。接收循环对每段增量输出执行该查询，累积延迟显著。
- 修复：新增传输日志索引 wire_request_direction_id(request_id,direction,id)，并对当前传输日志建立相同索引；不修改任务状态、请求、回执或正文，也不重发执行。
- 验证：实际查询计划变为 SEARCH USING INDEX，同请求约 4000 条消息的读取约 0.021 秒。当前作业继续执行。
- 后续：单有索引仍重复解析当前请求全部日志，积压使已在 provider 完成的数学审核触发接收超时。接收循环改为仅收到对应 turn/completed 时重建完成响应。
- 对账：新增 reconcile_completed，只读取 provider thread/read 的指定已完成 turn，并交叉核对 provider rollout 的 task_complete、原始最终消息及该 turn 的 token_count；保留文件哈希和行号证据，不重发、不伪造 turn/completed 通知。
- 后续接收优化：增量片段按最多 64 条批量提交；任何非增量事件、外发命令及关闭都会落盘，真实调用/完成回执不延迟到后续调用之后。
- 状态：索引和接收循环修复完成，`tests/compact/test_codex_bridge.py` 11 项回归通过，包含已完成恢复、未完成拒绝恢复和外发/完成前刷盘。

## INCIDENT-20260913-02：数学审核反馈证据未路由到 finding

- 实际完成的首次数学审核中，asset-context-efficiency、explicit-geometric-intuition、p01-image-size 三项 issue 引用了 p01-l03-s029 / p01-l04-s008，但 findings 没有这些页面，违反反馈问题必须路由的提交契约。
- 门禁正确拒绝接收，未把该结果算作审核成功。保留真实完成响应、runtime 与用量，调用既有 reject_completed 入口生成有界 submission_correction，由 runner 决定后续作业；没有由 main 改写审核判断或放宽校验。
- 状态：待原审核通道补正提交；属于同一审核轮的提交纠错，不是作者修订后的复审。
- 同类后续：教学通道 theme-and-source-contract-migration 的 needs_decision 未与 finding 页面相交，被正确拒绝。已按真实完成记录生成提交纠错，明确主题和兼容升级已有用户授权，不能制造新审批。
- 同类后续：语言通道 scaled-image-label-readability 的 needs_decision 未路由，解释又明确没有观察到实际缺陷。已交该通道补正证据边界与问题状态，不能伪造缺陷或可读性通过。

## BUG-20260913-06：审核提交纠错未收到拒收原因

- 复现：数学通道首稿真实完成但反馈定位被拒，reject_completed 生成第二 attempt；实际请求 packet 没有 submission_correction，导致同类问题再次出现。runner.packet 只为 write/edit/revise 添加纠错说明，而既有拒收入口也可登记已完成的审核结果。
- 修复：review 的新 attempt 也接收真实拒收原因和原始结果；保持无源码写权限，不复制作者输出，不增加审核轮次，不自动放宽审核校验。
- 状态：已修复；`test_review_correction_receives_rejection_without_writable_source` 通过，验证真实拒收原因和原结果进入同轮审核补正，且无源码写权限。此前已派发请求不能伪称收到纠错说明。

## 本轮验证汇总（2026-09-13）

- `PYTHONPATH=src .venv/bin/pytest tests/compact -q`：完整控制层测试通过，exit 0。
- `scripts/validate_project.py`、`scripts/check_documentation.py`：success=true；`git diff --check` 通过。
- 权限提升后的 `toolchain doctor`：原生 HTML/PDF 冒烟检查通过，3 页对应。
- 真实 Codex 桥接已完成只读诊断，具有真实 runtime、receipt 和 usage。上述测试不代表五通道审核完成，也不代表新版 p01 已交付。
- 当时依用户“等等”暂停后续课件工作，返修案停在 proposed v2。随后用户明确“我已查看TASK.md，可以了，继续吧”，已据此确认 v2 并恢复五通道审核。
- 当前统一主题原生回归检查通过：`/tmp/p01-theme-check-20260913/report.json`，5 页，包含原 p01 公式表格样例；无溢出、越界或公式丢失。此结果不代表返修整稿已通过检查。
- 已实际取得并阅读用户 Gitee 原稿的主题段；当前主题字号、标题配色和强调样式与该参考一致，表格另有明确边框和留白。

## 被拒收与补正相关的实际调用量（2026-09-13）

以下取自任务 SQLite `usage` 的真实 provider 回执；包含首读/brief 和执行。缓存输入是输入的子集，推理量是输出的子集，均不重复相加。首轮审核产生了有用问题，不能将下表全部称为浪费；主代理消耗未包含，未估算费用。

| 通道与 attempt | 输入 | 其中缓存输入 | 输出 | 总 tokens |
| --- | ---: | ---: | ---: | ---: |
| 数学首轮 a-f28a979393744e818b7ee2469be68bca | 715248 | 539904 | 6557 | 721805 |
| 数学第二次 a-f51b1300908d4119906baee2363f7be7 | 504449 | 389888 | 6377 | 510826 |
| 教学首轮 a-f6041c0e4512418b819638536a80e537 | 757082 | 560512 | 7098 | 764180 |
| 语言首轮 a-ea3a641d5a174135a7639a5d3aa80825 | 718526 | 541696 | 7510 | 726036 |

数学第二次调用未收到 BUG-20260913-06 应提供的拒收上下文，实际又发生同类拒收；这部分重复不能仅归咎审核者。另两个补正执行请求已排队但尚未外发；暂停外发，避免明知缺少上下文仍重复调用。数学第二次的真实完成响应已对账并通过既有 reject_completed 登记拒收；实际配置 max_attempts=2，runner 返回 budget_exhausted，并生成 content-retry-budget 决策。没有重置次数或擅自修改已确认配置；新版 p01 尚未交付。

## BUG-20260913-07：耗尽补正预算后没有登记单作业额外授权的入口

- 证据：数学审核第二次已真实完成并拒收，decision 10 为 content-retry-budget；max_attempts=2。用户明确批准仅增加 1 次补正，原程序没有与此对应的控制入口。
- 修复：新增 job authorize-content-retry，针对具体预算决策登记不可变授权事件，只增加该作业一个执行名额。重复处理同一决策不叠加名额，不改确认快照、不重置序号、不适用于未知执行。
- 验证：test_explicit_one_job_retry_does_not_reset_attempts_or_config 通过；第三次若仍拒收会再次阻塞。
- BUG-06 后续恢复：对宿主日志证明 sent=0、response 为空且 wire_count=0 的两个旧审核请求，runner 补入已有拒收说明；保存原请求、修正后请求和双向哈希审计，不改 TASK 上下文。已实际补齐教学、语言请求，新增模型调用为 0。已外发的数学第二次不追溯修改。
- 针对性验证：审核纠错、授权上限、未外发补充三项测试通过；桥接 11 项测试通过。此前运行中的完整 compact 测试也已 exit 0（该次全套不覆盖随后新增的三项针对性测试）。

- 实际恢复进展：教学第二次 attempt a-db1160f8dc81481f90179b3bf6802424 已在补齐拒收说明后 succeeded；尚不代表其他通道或交付完成。最新桥接与有界恢复测试文件合跑全部通过。

## BUG-20260913-08：旧 p01 获准插图尺寸与当前源码契约不兼容

- 证据：有效历史反馈 p01-image-size v2 允许本次 p01 使用规范 Marp 外部图片尺寸；当前 source_policy 的 IMAGE_OPTION 拒绝全部 w/h/width/height 尺寸指令，runner 的通用作者约束也没有任务级例外入口。教学通道已将其作为主题和源码兼容 finding 路由。
- 影响：不能直接把旧稿连同尺寸指令提交给新代码；“历史允许”不等于当前程序实际支持，不能伪称兼容已完成。
- 本轮处置边界：用户已授权适应当前代码，作者工作副本会采用当前固定主题，并收到 remaining_source_errors。可在保留图形数学含义和可读性的前提下迁移源码；若确需保留受阻尺寸指令，再实现有界授权入口，不能为单个任务放开全项目背景、滤镜、HTML 或 CSS。
- 状态：已定位并登记，待作者迁移与原生整稿检查验证；尚未宣称修复。

## BUG-20260913-09：明确的未解决返修问题因重复字段缺失而整份拒收

- 证据：数学第三次 attempt a-eaabd5f5ab5446da999bd5992e05a687 已真实完成；theme-and-source-contract-migration 在 repair_checks 中明确标为 needs_decision，带具体说明与四个实际页面，但未在 findings 再列一遍。程序因此拒收整份报告。
- 根因：需要“所有未解决问题送达作者”的控制目的，被实现成要求模型重复填写两个字段。问题、状态和位置并未缺失，却额外消耗完整模型执行机会。
- 修复：先完整验证原始结果、确认问题 ID 和实际源码页面；仅将未重复列出的 needs_decision 检查逐字路由为作者必须处置的项目。保存原字段来源、problem_id 和 reviewer_status，不猜严重度、不改变审核者原话或状态。原始 result_json 和 provider 回执保持不变，新增单独路由事件。
- 边界：不把未解决问题改成通过；作者仍须逐项处置，原生门禁不变。不为缺失页面、虚构引用、漏填确认问题或未知执行兜底，也不额外调用模型。
- 状态：已实现，真实结果重接收前进行针对性回归。

- BUG-09 验证完成：两项路由回归及原有反馈问题必须路由的回归通过；虚构页面仍拒收。实际数学第三次原始响应已成功接收，新增模型调用 0；问题原话成为 j-4923700ced5842a28244396ccb07ae7f:7，origin 明确指向 theme-and-source-contract-migration。没有第四次执行，没有改写原始审核结果。

- BUG-09 同族修复扩展：历史 feedback_checks 的 issue 也已被原程序要求重复写入 findings（见 INCIDENT-02 的早期失败）。现在同样先核实反馈 ID/版本及逐字源码引用，再原样路由，保留 issue、原始引文和字段来源，不猜严重度。空证据和虚构引用仍拒收。后续统一记录为 review.explicit_issues_routed；此前数学恢复的 review.repair_checks_routed 历史事件不改写。
- 根因判断更新：早期 INCIDENT-02 将重复字段漏写主要记为提交失误；如今确认该契约会把已完整陈述且有源码证据的问题整份拒收，是重复调用的程序诱因。缺失纠错上下文（BUG-06）则进一步放大了代价。

- 用户额外批准的数学第三次实际用量：attempt a-eaabd5f5ab5446da999bd5992e05a687，2 条调用回执（含 brief），输入 314212，其中缓存输入 199936，输出 5491，总 tokens 319703。之后的原始结果重接收与无损路由没有新增模型调用。

## BUG-20260913-10：受众源码引文省略数学定界符被误判为虚构

- 证据：audience:a-bf49a2003d1a44309a5697f35cd2361d:16 的真实完成响应引用“e_2 的像是原点”，源码为“$e_2$ 的像是原点”；其余文字完全一致。严格子串检查拒收整步。
- 修复：保留逐字匹配；仅允许成对行内数学定界符省略，公式内容、页面范围及其余文字仍核对。原始响应与源码不改写，转义美元符号和显示数学不按此规则剥离。
- 恢复：针对性测试后重接收原始已完成响应，不重发模型请求，不跳过阅读步骤。
- 验证与实际恢复完成：受众流程测试文件全部通过；原始步骤 16 已接收并继续步骤 17、18，没有新增纠错模型调用。原始引文、response、receipt 未改写。

- 本轮回归：完整 `tests/compact` 已 exit 0，全部通过（包含 BUG-06/07/09 最终代码）；随后 BUG-10 的整个受众流程测试文件也已 exit 0。没有把程序测试等同于新 p01 的教学或原生整稿验收。
- BUG-10 同族：真实步骤 28 将“两个方程、两个未知量也不一定有唯一解：”引为句号结尾，其余逐字一致，被原始子串规则拦截。仅允许引文末尾中文句号/冒号互换，内部标点和全部文字保持核对；不把删掉否定词、改写命题或截断后续文字视为一致。原始结果保留，针对性测试后重接收。

## 用户明确的拒收处置要求（2026-09-13）

- 用户指出：“提交给拒，是bug, 要记录下来。如何临时绕行，是你自己就能处理好的，为什么要丢回给reviewer？”
- 处置责任在 main/控制层：先登记程序拒收，核实真实执行与原始响应，对已有合法语义结果采用可审计临时绕行或修复接收规则；不因提交接口拒收就重发 reviewer。
- 原始意见、问题状态、源码证据和真实回执必须保留；不得为通过而改写 reviewer 结论或伪造完成。当前数学/教学/语言中由重复字段契约及缺失纠错上下文引发的额外调用，是已登记的可避免损耗。
- 本次后续两类引用格式误判均已通过修复接收规则重用原响应，未追加 reviewer 调用。后续不再把纯程序拒收当成 reviewer 重做理由。

## BUG-20260913-11：旧输入预算阻断受众汇总并错误终结已有分段 attempt

- 证据：受众 attempt a-bf49a2003d1a44309a5697f35cd2361d 已完成全部 44 步，汇总包 275309 bytes；事件 860 仍按旧默认 262144 拦截，run_dispatched=0、provider_receipt=NULL，却把 attempt 标为 failed。有效反馈 asset-context-efficiency v1 已明确记载用户授权“起码应当允许 1M 上限”。
- 根因：输入预算授权只留在语义反馈中，机械预算未应用；本地未派发包失败被当成结束 attempt，没有保留同一次阅读的恢复入口。
- 修复：通过独立授权事件应用 1048576 bytes，不重写配置快照和模型配置；提供严格限于最后一次、有匹配本地失败事件、没有 final 外发/回执、无未知分段的原 attempt 恢复 API。保留全部已完成分段与历史错误，解决 input-boundary 记录，不新建审核 attempt、不重复试读。
- 不截断原始 findings 或所需正文；不将真正已派发或未知执行伪装成未执行。

## BUG-20260913-12：受众汇总要求模型重复抄写已有分段 findings

- 证据：Runner.execution_request 的 synthesis 指令要求 Preserve the earlier student/production-language findings verbatim，并再次内嵌 44 段结果。实际首次汇总请求 a-bf49a2003d1a44309a5697f35cd2361d 已输出 37734 字符时仍在生成。
- 影响：已有结构化意见的逐字保留本应由程序完成，让模型重新输出放大输出 tokens、等待时长和转录错误风险。不能将此描述成重新获得的独立审核价值。
- 本轮临时处理：真实汇总已在执行，不打断、不重发；原分段意见保留。后续代码应把不可变分段 findings 作为程序路由的数据，汇总模型只处理尚需语义判断的反馈对应和结论，避免复制生成；不得借此删去意见或跳过五通道。
- 状态：已登记设计缺陷，当前不为修复此性能问题重新派发正在完成的审核。

## BUG-20260913-13：宿主固定总时限中断仍在工作的作者

- 作者 attempt a-81c6f41168db498887235b01a8f41a56 的 turn 01a09992-263a-7013-a0c7-4bfbfb37a2a0 在持续输出期间撞到 bridge 固定总时限；异常退出关闭 app-server。随后真实 thread/read 证实 status=interrupted，未完成提交。
- 修复：运行等待改为无消息时限；RPC 仍有时限。只有提供方明确证实 interrupted 的可写作者作业才允许在原线程、原输出上接续；未知状态禁止重发。保存原始回执和逐调用 usage，不增加审核轮次。

## BUG-20260913-14：程序验收待办被当成语义决策，阻断自身前置检查

- 作者正式提交 r-e727c6a6e1124a19b2335176e66a1c7d 后，5 个 resolution 与 2 个 repair_check 明确仅等待统一主题、图片及表格的原生程序证据；workflow 在 revising 阶段因 needs_decision 阻断，尚未到 full gate。
- 临时绕行：对同一不可变 artifact 调用正式 Quality full gate，保留原报告与状态。没有把待检查声明改成 addressed，没有追加 reviewer。
- 另有作者自己声明的 SVG 数值核验未完成，属于作者待办，不能由 main 冒充 reviewer 后代签完成。后续派发必须携带原提交及程序证据，只补已有未结项。

- BUG-20260913-14 处置修正：用户进一步指出不要默认所有要求合理。main 重新判定：作者没有报告具体图形错误，“未穷尽独立复算全部 SVG 路径”属于核验范围说明，不值得据此追加作者调用。当前 full gate g-55321731881f424999a661e909fae8bc 已真实通过。
- 增加独立的 verification-boundary 决策记录：必须指定原报告全部实际未结项，绑定原报告哈希、精确 revision 和真实通过的 full gate；保留原作者状态与解释，不冒充作者修正、不伪造用户同意。该入口由 main 明确作出范围判断，不能由程序自动把 issue 清零。
- 验证：控制层测试确认缺少 full gate 或漏列未结项时拒绝；有明确决策后不新增任何模型 attempt 即可交付；修改原报告使决策失效。测试使用模拟渲染，仅检验控制层；真实 p01 原生检查另有上述 gate。

- 本轮最终状态：r-e727c6a6e1124a19b2335176e66a1c7d 的 194 页 PDF 已按原生 full gate 发布为 p01 修订版，repair package 为 ready。边界决策之后未新增作者或 reviewer 调用，原作者报告保持原样。

## BUG-20260914-01：数据库自动迁移没有审计记录，无法追溯版本变化

- 用户要求：保留自动迁移能力，但自动迁移不留下记录是 bug，需登记。
- 证据：v0.8.10 的 `src/mpres/control/store.py` 中 `Store.connect()` 在打开旧库时依次执行迁移 SQL、更新 `PRAGMA user_version` 并提交事务；8→9→10→11 各步没有记录迁移时间、触发入口或执行者。状态查询也会经过该入口，因而可能写入数据库。
- 本次观察：`tasks/linear-algebra-v7/.mpres/task.sqlite3` 当前为 schema 11，新版分稿表存在。用户指出原库应为 schema 8；main 本会话执行过 `task status`、`task policy-show`、`runner outstanding`，但首次 CLI 调用前未记录版本。无法独立确定是本轮哪次调用、启动器还是更早的调用触发迁移；不能据当前版本编造历史执行者或时间。
- 影响：用户无法区分既有新版库与查询时自动升级；出现迁移问题时缺少准确版本链和触发来源。文件修改时间会被后续写入覆盖，不能替代迁移日志。
- 修复要求：为实际迁移持久记录起止 schema、时间、应用版本、迁移脚本标识或摘要及可取得的触发入口、进程和执行身份；无法取得的身份明确写未知。成功记录应与对应迁移事务一致，失败应保留可追溯的失败证据且不得记为成功。CLI 应明确报告本次是否发生迁移。历史迁移只能标为历史未知，不追溯伪造审计记录。
- 验收：覆盖 schema 8→11 的逐级成功、重复打开不重复记迁移、并发打开仅实际执行者记成功、失败回滚不产生成功记录，以及状态查询触发迁移时的提示；原任务配置、runtime、发布记录和生产授权保持原有语义。
- 状态：已登记，尚未修复；本次仅记录问题，未修改迁移代码。

## BUG-20260914-02：新写作自动复用旧稿，缺少明确弃用后的重做入口

- 证据：`control/planning.py:source_artifact` 优先返回已有成功稿或导入稿；`control/runner.py` 为 write 包加入 existing_unverified_source、要求复用的约束，并调用 prepare_edit_source 复制旧稿。现有 plan 分稿接口也继承该基线，没有显式从指定参考资料重新写作的选项。
- 实际冲突：linear-algebra-v7 已确认 TASK 修订 897 要求弃用 p01 之外的未交付内容，并参考 sources/v4-deliverables 对应文档重做 p02、p03。仅更新 TASK 不能阻止上述自动复制，也不能用删除历史 artifacts 或手工改数据库来假装完成弃用。
- 修复要求：提供正式、可审计的重做基线选择，绑定明确授权和范围，保留历史记录；新写作仅获得指定参考资料和当前主题，不自动带入弃用稿或复用指令。保留失败后对本次新稿的有界修正能力，不能每次重试又清空新成果。范围外和默认续写行为不变。
- 状态：已修复。plan 的显式 source_mode=fresh 将重做范围与 artifact 截止位置纳入呈现和确认记录；source_artifact 排除此前旧稿并保留本次后续成功稿。原分稿回归 27 项及新增重做/完整批次针对性回归 3 项通过；测试使用确定性语义/渲染替身，不代表真实课件验收。数据库 schema 未变更。

- BUG-20260914-03 当前任务处置：TASK 修订 1020、recent-real-world-examples v2、v7-user-repair v3 已按用户明确要求登记；项目共同教学边界和新任务默认反馈同步生活算例合理性要求。旧反馈和作者报告保留。当前稿件尚待正式作者编辑删除出处，不宣称已完成改稿。

## BUG-20260914-04：运行中未冻结稿缺少需求变更后的安全恢复入口

- 证据：p02-01 阻塞于来源核验，用户明确取消该要求；现有 policy 只允许暂停任务修订，但无暂停入口，workflow.retry-checks 又只处理已失败的机械 gate，无法把新要求交回作者。
- 修复：新增有审计事件的 policy-pause/resume 和 edit-after-amendment。暂停拒绝在途/未对账执行；编辑恢复限于活动批次中未冻结且阻塞于 preflight 的候选，并要求更新的正式 TASK 修订。原报告、源码修订及回执均不改写，新编辑仍走完整门禁与首轮审核。
- 验证：针对性控制层测试检查有在途时暂停回滚、原批次/runtime 保留、未确认新要求时拒绝编辑恢复及报告不被改写；渲染替身不代表真实课件验收。
- 实际应用与验证：三项新增恢复测试通过；guidance routing、deck workflow、effective policy/batches 合计 66 项回归通过，文档检查 success=true，git diff --check 通过。p02-01 已经正式创建 amendment edit 作业，原批次恢复；真实作者 brief 的上下文回执确认收到 TASK 修订 1020。上述不代表新稿已通过教学审核或交付。

## BUG-20260914-05：后台生产退出未通知主会话，任务停滞需用户主动发现

- 用户报告：后台已经结束，却没有唤醒主代理。
- 实际证据：本次 bridge 进程退出码为 2；保存日志最后含 p02-01 语言审核请求 a-037fa791973944d9b9886f01110b013d 的 response_rejected，原因 Feedback evidence is not in the submitted/frozen source。任务数据库仍为 running，两份 p02 停在 reviewing，p03 尚未开始；因此“进程结束”不是“交付完成”。
- 已确认的项目实现：CodexBridge.drive 在一批请求中存在未接受结果时返回，CLI 对 response_rejected 返回 2。当前入口未建立面向主会话的持久结束通知、确认接收和唤醒交接；内部线程条件变量 notify_all 不等于通知主代理。不能据此推断宿主本来承诺了自动唤醒，也不能只写一个日志就宣称唤醒已实现。
- 主代理执行失误：上次在异步进程仍运行时结束回复，没有建立可靠的后续接管机制；让用户自行发现停止。此前仅说明“仍在运行”不能代替持续推进责任。
- 修复要求：为正常完成、异常退出、待对账/拒收和循环上限分别持久记录明确终态与下一步；与宿主真正支持的通知/唤醒能力接线，具备送达/确认、幂等及重启恢复。没有可用唤醒能力时，主会话保持接管并等待进程结果，不能暗示退出后会自动恢复。通知不能触发未知执行的盲目重发。
- 状态：已登记；尚未实现宿主级可靠唤醒。当前由主代理读取真实结果、处理拒收后继续原批次。

## BUG-20260914-06：唯一逐字引文填错页 ID 导致整份审核拒收

- 实际证据：p02-01 语言审核引用“ 两图显示范围不同。”（原回执无前导空格），将页 ID 填为 p02-l07-s015；原句在同一冻结稿另一页中唯一存在。其余证据与结论保留，不将页位错误描述成虚构引文。
- 修复：接收层仅对现有合法页 ID 上不匹配、但同一冻结稿中唯一逐字匹配的反馈引文纠正坐标；不存在、多个匹配、非法 ID、改写引文仍由原校验拒收。规范化不改引文、状态、解释、findings 或原始响应；成功接收时另记 review.exact_quote_relocated，绑定 attempt/artifact 和原/新页位。
- 恢复：通过原始回执 replay 接收，不重发 reviewer，不增加审核轮次。
- BUG-20260914-06 验证：review references 测试 13 项通过；实际原始回执已 replay 接收，审计记录精确为 p02-l07-s015 → p02-l07-s010，未新增模型调用。

## BUG-20260914-07：保留的旧桥接回归测试未适配新版接口

- 完整 compact 回归发现 tests/compact/test_codex_bridge.py 仍直接调用 CodexBridge.enqueue，当前新版方法已在独立 journal 对象上；首个失败为 AttributeError。该源码文件和测试文件本轮均无改动，不能把整个测试集宣称通过，也不能仅据旧测试失败判断正在工作的真实桥接不可用。
- 状态：已记录这项确定的接口不一致；完整回归其他失败仍按实际结果另行归因，不能全部预先归入同一原因。后续需把旧不变量测试适配到当前 journal/bridge 边界，不为通过测试恢复弃用接口或删除测试意图。
- 完整回归补充：运行中已出现 15 项失败后停止扩展回归（KeyboardInterrupt，未跑完整套）；13 项位于旧 test_codex_bridge.py，另 2 项 fake-server 增量桥接测试报告 Codex disconnected，后两项根因尚未确定，不能按旧 API 问题合并解释。日志 /tmp/v7-full-control-regression.log；此前针对性 13 项和恢复相关 66 项通过的结论不变，不宣称完整回归通过。

## BUG-20260914-08：审核逐字引文因 Markdown 换行差异被拒收

- p02-01 audience 步骤 2 引用 p02-l08-s011 的相邻两句，响应合并了源码中的换行；其余文字逐字一致，却被判定为 invented。
- 接收校验现在忽略空白差异，仍要求同一已派发页存在完整引文；不修改原始响应、引用或审核结论，不放行改写或跨页证据。原始回执通过 replay 恢复，不增加审核轮次。

## BUG-20260914-09：资源清单“只读”说明遮蔽了 TASK 已有绘图授权

- p02-02 作者已经修订 8 项 findings，但把 reproduction_source 清单“Read as source only”理解成不得执行 Python，导致三维图标签/留白修订 needs_decision。TASK 已允许 Python 绘图，无需用户再次授权。
- 清单改为先检查源码，并遵从 TASK 已有授权，在工作副本中执行，保留归档输入。同步删除通用作业约束与语言 rubric 中残留的生活算例新闻/出处强制要求，遵从用户已确认规则。
- 新增有界 author decision continuation：仅活动批次中已经完成的 blocked/revising 作者，取最新成功输出继续；保留旧结果与 findings，不增加 reviewer 轮次；新作业明确携带 main 对既有授权的解释，并记录审计事件及重试预算。不是伪造新用户批准或把 needs_decision 改成 addressed。
- BUG-20260914-08/09 验证：audience/effective-policy 组合中其余 33 项通过；新增 continuation 测试最初因测试准备未暂停批次失败，修正 fixture 后单独通过，验证最新输出基线、原 findings/报告保留、重复调用拒绝、决定进入新 packet、5 个原审核作业保持不变并完成交付。仅测试渲染使用替身，不代表真实浏览器验收。真实 audience 步骤 2 的原回执已 replay 接收；p02-02 已创建 j-f8ec169bc07a4134bfdd72b08d045fd9，基于 r-5dece74191af4c259d0de86877229339。
- 后续步骤 4 又因“第二行加回第一行的两倍。”与源码同句末尾冒号的差异拒收。扩展的规范化仅允许引文最后一个中文句末分隔符（。；：）互换，不改中间标点、字词、数学符号、页范围；新增测试覆盖错误运算和实词仍拒收。接受格式差异时另记 audience.quote_format_normalized，保留原始响应与原文。

## BUG-20260914-10：TASK 点名的本地教材未进入作者资源清单

- p03 整稿作者已完成 70 页编辑，但 v7-user-repair 报告缺少五本文献及同济教材阅读证据，冻结前被阻塞。六份 TXT 实际都在本地；仅课次 sources 进入 packet，使 TASK 中明确点名的文本资料没有按需资源入口。
- 修复：从已确认 TASK 的显式反引号 `sources/*.txt` 引用构建不可变快照，并以 approved_reference 按需提供；不扫描整个任务目录、不内联整本教材、不宣称有文件即已阅读。路径继续受 task 内 regular-file 和不可变快照检查约束。
- 作者 continuation 扩展至尚未冻结、已有成功 edit 输出但阻塞于 preflight 的输入缺口。取当前成功候选继续编辑，提供 main 对既有要求的说明；不要求用户再次批准原本已经要求的教材，不改旧 issue 报告、不绕过新的作者检查与首轮审核。
- BUG-20260914-10 验证：context resources 与两项作者 continuation 回归共 20 项通过，覆盖教材不自动内联、只读快照、已有稿件/原报告保留及正常恢复交付。当前真实 p03 仍等待使用更新 packet 的作者执行，不能把测试通过记为教材已读或课件已交付。

## BUG-20260914-11：transform 图例固定命名阻止作者完成图文一致性修订

- p02-01 六项 audience finding 中三项涉及复合变换、逆变换和 L 变换的图例对象身份。作者核对了坐标，但当前生成器固定 v_i/Av_i，数学输入拒绝标签字段；手改 SVG 又会破坏确定性检查，因此正式报告 needs_decision。
- 修复：transform 的 version 1/2 增加可选 labels:{inputs:[...],images:[...]}，两个数组长度必须与 vectors 一致，只允许有长度限制的简单数学名；不接受样式、HTML、坐标或结果字段。生成器仍自行精确计算像坐标，标签纳入 SVG 复现比较；未提供 labels 的旧规格保持原输出。
- 验证：computed figures 30 项测试通过，包括标签数学坐标不变、两版真实 Matplotlib 复现、改标签不重建时拒绝、数组长度与样式拒绝。此处真实资产测试不是原生 Marp 布局验收。
- 实际续修：p02-01 j-58bf631f66854cd2ad391df4ada41b99 基于 r-eb0cb6bdb95b456b9d65cf5e8258c4b9；p03 教材续修 j-585739e4d80a4302b1adf612d97a7c4a 基于 r-5c0a925be0bf48319faf290b5f496ae7。保留上一轮修改及回执，未伪造完成或重跑 reviewer。
- 实际验证补充（BUG-20260914-10/11）：p02-01 续修报告包含六份教材的具体章节/行段与用途，五幅图使用新标签接口重新生成，作者 findings 全部处置并经真实后检正式交付；ready 条目为 r-d4a13cb91f8d4edcb4294afcb807e195，警告报告保留 1 条小字提示。p03 教材续修也已成功接收并越过教材反馈门禁，后续由运行器处理独立的五页 HTML 溢出，不将该版面问题归为教材输入仍缺失。
- BUG-20260914-08 后续：p03 audience 步骤 2 将独立公式引用为行内公式，并把末尾英文句点移到数学环境外作为中文句号；公式内的数值、负号、分数、算符逐字一致。证据匹配扩展为忽略美元数学分隔符、允许末尾英文句点同中文句末分隔符对应，仍保留公式内容（含小数点、负号）及同页约束。针对性测试覆盖独立/行内对应、负号改动拒绝和 1.2 不等于 12；原回执与规范化事件保留。
- BUG-20260914-08 步骤 3：完整分句“整个矩阵乘 k，相当于每行都乘 k”因原文末尾逗号后接“所以”、引文改用句号而拒收。将允许的引用边界标点补齐中英文逗号/分号/冒号；仅引文末尾可对应，内部文字、标点和数学内容仍不可改变。新增测试验证 k 换成 n 仍拒绝。

## v0.9 累计源码落实

原登记保留，不追溯改写当时观察。实现/回归和部署边界见 [落实索引](RESOLUTION-v0.9.md)。
