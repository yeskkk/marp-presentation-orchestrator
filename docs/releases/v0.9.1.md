# v0.9.1 — 练习覆盖与输入成本防线

累计包含 v0.9.0。新增 exercises.json 轻量索引、作者题页自检清单、教学法/学生视角的逐题结构化覆盖；题页可隔离读取，不提供答案、备注或前页实例。程序只验证覆盖及引用，不冒充数学自足性判定。已有提交与旧派发请求不追溯拒收；新编辑/返修必须使用新契约。

统一保守引文比对：只容忍数学定界符、排版空白和句末标点；不改数字、符号、否定或词界。唯一同稿定位保留原始报告及迁移证据。

开始付费 brief 前检查已明确要求的本地资源及已知图形规格。教材/参考文本改为任务级内容寻址不可变缓存加逐次轻量固定清单，保留旧读取/反馈回执及必要真实阅读，不把“存在”当成“已阅读”。

新增 CLI：`exercise candidates|check|isolate`，`resource preflight`。无新增 reviewer 通道，无真实模型调用。legacy 模板未修改或专门测试。

验证：compact 全集按模块分四组执行，共 617 项通过（145+165+170+137）；不代表真实模型/浏览器验收。
