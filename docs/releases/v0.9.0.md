# v0.9.0 — 运行修复基线与教学硬规则

合并 v0.8.10 运行副本的 24 项有效源码差异；legacy 模板不改、不为它增加测试。
练习单页独立规则进入写作、编辑、教学法、学生视角及分段阅读的活动输入。
编拟生活算例原则吸收为未来任务通用要求，消除写作指南和 compact TASK 模板冲突。
迁移失效桥接测试到 Journal/Bridge/Runner，保留不重发、运行时匹配、原始证据和路径隔离断言。
修正测试子进程从临时工作目录导入测试宿主失败；测试替身不是生产模型/渲染器。

此版不修改任务数据库、不返修实际课件。后续小版本加入覆盖索引、维护与观测能力。

验证：compact 回归 593 项通过；使用确定性测试宿主，不代表真实模型或原生浏览器验收。
