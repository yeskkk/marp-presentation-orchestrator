# v0.9.3 — 启动权限、前台接管和可复核计量

累计包含 v0.9.0–0.9.2。新增启动前权限选择及显式参数、取消与完整访问二次确认；所有平台启动器共用入口。
只显示实际传入请求，不伪称突破宿主限制；不改任务 runtime、生产授权或现有 worker。真实 Windows/浏览器仍需部署验收。

新增只读 report usage（JSON/Markdown/CSV）、程序报告 schema、逐课件/角色/操作及调用明细。
缓存/推理子集不重复加，缺失不当零。已留存供应端时间与适配器观察分开，墙钟采用区间并集；等待原因显式记录。
在原任务上只读重现三个课件108次/40,840,004token、供应端累计14,427秒、并行覆盖7,940秒、交付跨度12,270秒。

runner/bridge 终态或预算用尽以有界前台交接返回 main，持久去重、人工处理 ack、非零退出；Ctrl-C 保留接管线索。
没有宿主原生推送就不宣称后台唤醒；SIGKILL 不可捕获，重开必须看未决请求。新增 main-only runtime-operations，
不增加教学 agent/审核通道或所有 worker 的必读输入。完整升级和选定三个课件返修说明随包提供。

验证记录在 validation-v0.9.3.txt；确定性替身、PTY、只读原任务用量和隔离数据库测试不是实际模型课件验收。

验证完成：完整 compact 662 项通过（155+176+194+137），最终四组退出码均为0；静态/文档检查通过。
