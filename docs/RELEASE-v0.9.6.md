# v0.9.6

当前版本检查点贯通整个任务目录，不再只精简数据库。目录预览、精确计划应用、可恢复阶段、历史文件 tombstone、最小通信投影、索引重建、净占用核算，以及显式策略下的自动安全点收尾均已加入。迁移至 schema 13，保留配置、授权、计量及实际执行事实。

同 ID 的 delivery_part 是保留原稿，不是替代父稿。当前发布识别和公共目录导出共用正确边界；旧任务移动到新路径后不复用旧索引 cursor，而是重新构建投影。

验证包括：完整关闭任务清理后再次走返修流程、请求不重发、计量不变、旧索引删除/搬移后重建、三处中断恢复、活动租约与变更计划拒绝，以及缺 PDF 归档明确不声称交付就绪。协议/门禁 fixture 不是实际模型或原生 Marp 渲染。
